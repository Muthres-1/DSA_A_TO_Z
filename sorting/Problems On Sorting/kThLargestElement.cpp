#include<unordered_map>
#include<unordered_set>
#include<algorithm>
#include<iostream>
#include<string>
#include<vector>
#include<math.h>
#include<stack>
#include<queue>
#include<list>
#include<set>
#include<map>
using namespace std;

int kThLargestEle(vector<int>arr,int length,int k){
    if (length<k)
    {
        return -1;
    }
    int k
}
int main()
{
vector<int>arr={};
int k;
cin>>k;
cout<<kThLargestEle(arr,arr.size(),k)<<endl;
return 0;
}