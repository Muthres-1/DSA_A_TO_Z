#include<unordered_map>
#include<unordered_set>
#include<iostream>
#include<string>
#include<vector>
#include<math.h>
#include<stack>
#include<queue>
#include<list>
#include<set>
#include<map>
using namespace std;
int main()
{
// Everything is same as map,only thing it can store also duplicate keys as well but everything is in sorted order 
// only thing mpp[key] cannot be used here
// Something is similar to multiset irrespective of keys


return 0;
}