#include<unordered_map>
#include<unordered_set>
#include<iostream>
#include<vector>
#include<stack>
#include<queue>
#include<list>
#include<set>

using namespace std;
int main()
{

    // there was a set and multiset and in set it follows sorted and unique with O(log(N)) while multiset follows only sorted and now we 
    // will discuss about unordered_set whihc just follows unique Everything is in O(1)
    // only worst O(n) which almost not happen or may happen some time ,not everytime 
    // all function work only upper_bound and lower_bound don't work
    // It stores unique but not in sorted manner random order 
unordered_set<int>us;
// It don't have even order like array it may possible that we inserted anyelement at starting and it may come at mid 


return 0;
}